---
title: 多规约设计
tags:
  - design
  - protocol
  - architecture
aliases:
  - Multi-Protocol Design
created: 2026-05-23
---

# 多规约设计

## 设计原则

1. **协议抽象层**: 可插拔的 Protocol 接口
2. **数据层复用**: `library.Store` 保持不变
3. **配置向后兼容**: `protocol` 字段默认 `"iec104"`
4. **点表向后兼容**: Modbus 专属列可选解析

## Protocol 接口

```go
type Protocol interface {
    Name() string
    Start() error
    Stop()
    ClientConnected() bool
    ClientAddr() string
    Stats() (interrog, control, spont int64)
    Uptime() int64
    Publish(point *config.Point)
    SetStore(store *library.Store)
}
```

## InstanceConfig 扩展

```go
type InstanceConfig struct {
    Protocol     string              `json:"protocol,omitempty"`
    ModbusConfig *ModbusInstanceConfig `json:"modbus_config,omitempty"`
}
```

## 目录结构

```
pkg/protocol/
  protocol.go        Protocol 接口定义
  factory.go         协议工厂
  iec104_wrapper.go  IEC104 适配器
  modbus/
    tcp_server.go    Modbus TCP 服务端
    handler.go       功能码分发
    converter.go     数据类型转换
```

## 开发阶段

| 阶段 | 内容 | 工时 |
|------|------|------|
| P0 | Protocol 接口 + 工厂 + IEC104 适配器 | 0.5d |
| P1 | Modbus TCP 服务端 | 2d |
| P2 | 数据模型 + Excel 加载器 | 0.5d |
| P3 | Manager 适配 | 0.5d |
| P4 | 前端规约选择 | 1d |
| P5 | 前端详情页 | 0.5d |
| P6 | 测试 + 文档 | 1d |

总计：6 人天

---

相关笔记：[[协议支持/多规约架构|多规约架构]]、[[协议支持/Modbus TCP 协议|Modbus TCP 协议]]
