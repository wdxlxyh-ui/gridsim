# GridSim v3.3.5 发布说明

- 标签：`v3.3.5`
- 日期：2026-08-15

## CSV 多测点回放映射修复

- 修复 Modbus 等协议中合法 IOA `0` 被前端当作「未映射」并在保存时跳过的问题。
- CSV 映射行现在保存原始 CSV 列号，拖拽排序或删除映射行后，源 CSV 列与目标测点仍保持正确对应。
- 自动匹配、已映射状态、保存、停止后恢复配置均支持 IOA `0`。
- 新增多测点 CSV 回放回归测试：首个数据列映射 IOA `0`，后续列映射其他 IOA，所有列按时间正确回放。
